# Arbitrary comment

machine:
  ruby:
    version: 1.9.2-p180
  environment:
    FOO: bar

deployment:
  master_merge:
    branch: 'master'
    commands:
      - git push -f origin master:deployment/staging

#  python:
#    version: 2.7.3

#deployment:
#  staging:
#    branch: master
#    heroku:
#      appname: quiet-depths-3134
