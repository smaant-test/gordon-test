machine:
  ruby:
    version: 1.9.2-p180
  environment:
    FOO: bar

dependencies:
  pre:
    - rvm 1.9.2-p180 do gem update --system
  cache_directories:
    - ~/.rvm/gems

#  python:
#    version: 2.7.3

#deployment:
#  staging:
#    branch: master
#    heroku:
#      appname: quiet-depths-3134
